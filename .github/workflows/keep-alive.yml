name: Keep Supabase Alive

on:
  schedule:
    - cron: '0 12 * * 1,4' # Runs every Monday and Thursday at noon UTC
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend
        run: curl -s https://serve-backend-pj5ndaw77a-rj.a.run.app/api/writers
